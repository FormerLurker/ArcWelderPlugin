When selected, Arc Welder will overwrite the original file with the compressed version.

Arc Welder *cannot* overwrite the source file if it is printing, and will return an error.

Default Value: disabled.